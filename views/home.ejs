<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <% if (user) { %>
        <h1>Welcome <%= user.username %>!</h1>
        <a href="/logout">Logout</a>
        <% if (user.role === 'freelancer') { %>
            <button id="startTracking">Start Tracking</button>
            <button id="stopTracking">Stop Tracking</button>
            <button id="toggleVideo"><%= user.isVideoEnabled ? 'Disable' : 'Enable' %> Video</button>
            <div id="timeDisplay">Time: 00:00:00</div>
            <% if (user.isVideoEnabled) { %>
                <video id="videoStream" controls autoplay></video>
            <% } %>
        <% } %>
    <% } else { %>
        <h1>Welcome to the Time Tracking Tool</h1>
        <a href="/login">Login</a> or <a href="/register">Register</a>
    <% } %>
    <script src="/script.js"></script>
</body>
</html>
